---
- hosts: all
  become: yes
  tasks:
    - name: install apache server on ubantu server
      apt:
        name: apache2
        state: present      
      when: ansible_os_family == "Debian"

    - name: start apache2 service
      service:
        name: apache2
        state: started
      when: ansible_os_family == "Debian"
